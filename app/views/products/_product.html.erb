<tr>
    <td>
        <% if product.photo.attached? %>
        <%= image_tag product.photo, style: "width: 120px; height: 150px;" %>
        <% else %>
        <%= t('products.index.no_photo') %>
        <% end %>
    </td>
    <td><%= product.title %></td>
    <td><%= product.description %></td>
    <td style="text-align: right;"><%= number_to_currency(product.price, unit: "$") %></td>
    <!--
    <td>
        <%= link_to t('products.actions.show'), product, data: { turbo_frame: "_top" } %>
    </td>
    <td>
        <%= link_to t('products.actions.edit'), edit_product_path(product), data: { turbo_frame: "_top" } %>
    </td>
    <td>
        <%= link_to t('products.actions.destroy'), product, 
            data: { turbo_method: :delete, turbo_confirm: t('products.index.confirm_destroy') } %>
    </td>
    -->
</tr>
