<nav class="flex items-center justify-between p-4 text-black bg-green-500 font-semibold">
    <!-- Logo y Nombre de la App -->
    <div class="flex items-center">
        <%= link_to products_path, class: "flex items-center" do %>
        <%= image_tag "logo.png", alt: t('app.name'), class: "h-10 w-auto mr-4" %>
        <h1 class="font-bold text-lg text-green-500"></h1>
        <% end %>
    </div>

    <!-- Enlaces de Navegación -->
    <div class="flex items-center gap-4">
        <% if Current.user %>
        <%= link_to t("categories.index.title"), categories_path, class: "block px-3 py-2 rounded-md hover:bg-green-100 hover:text-green-700 transition duration-200 text-gray-900 font-medium" if Current.user.admin? %>
        <%= link_to t("products.index.add"), new_product_path, class: "block px-3 py-2 rounded-md hover:bg-green-100 hover:text-green-700 transition duration-200 text-gray-900 font-medium" %>
        <%= link_to "Mi Perfil", user_path(Current.user.username), class: "block px-3 py-2 rounded-md hover:bg-green-100 hover:text-green-700 transition duration-200 text-gray-900 font-medium" %>
        <%= link_to t("navigation.logout"), session_path(Current.user.id), class: "block px-3 py-2 rounded-md hover:bg-green-100 hover:text-green-700 transition duration-200 text-gray-900 font-medium", data: {
            turbo_method: :delete,
            turbo_confirm: t("authentication.sessions.notices.confirm")} %>
        <% else %>
        <%= link_to t("navigation.login"), new_session_path, class: "block px-3 py-2 rounded-md hover:bg-green-100 hover:text-green-700 transition duration-200 text-gray-900 font-medium" %>
        <%= link_to t("navigation.register"), new_user_path, class: "block px-3 py-2 rounded-md hover:bg-green-100 hover:text-green-700 transition duration-200 text-gray-900 font-medium" %>
        <% end %>
    </div>
</nav>